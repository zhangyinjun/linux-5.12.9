name: check upstream patches

on: [pull_request]

jobs:
  patch_chk_job:
    runs-on: ubuntu-latest
    name: Patches Check
    steps:
    - name: Get PR info
      id: 'get-pr-info'
      uses: Brymastr/pr-info-action@v1

    - name: Set env var
      run: |
        COMMIT_CNT=$((${{ steps.get-pr-info.outputs.commits }}+1))
        echo "COMMIT_CNT=${COMMIT_CNT}" >> $GITHUB_ENV

    # no coccinelle pkg in ubuntu-latest(20.04 now)
    - name: Install deps
      run: |
        wget --no-verbose http://archive.ubuntu.com/ubuntu/pool/universe/c/coccinelle/coccinelle_1.0.8.deb-4_amd64.deb
        sudo apt-get -q=2 install build-essential gcc-10 bc flex bison sparse python libpython2.7 libelf-dev
        sudo dpkg -i coccinelle_1.0.8.deb-4_amd64.deb

    - name: Check out repository
      uses: actions/checkout@v2
      with:
        ref: ${{ github.event.pull_request.head.sha }}
        fetch-depth: ${{ env.COMMIT_CNT }}

    - name: Configure & Build Kernel
      run: |
        cp -p .github/deps/local_defconfig ./arch/x86/configs/
        make -s local_defconfig
        make -s -j"$(nproc)"

    - name: Run check scripts
      run: |
        cp -p .github/deps/commits_check.sh ./
        cp -p .github/deps/xmastree.py ./
        ./commits_check.sh ${{ steps.get-pr-info.outputs.commits }}
